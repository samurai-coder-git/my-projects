#include <iostream>
using namespace std;
int main()
{
    int length;
    int width;
    cout << " enter the length of rectangle \n";
    cin >> length;
    cout << " enter the width of rectangle \n";
    cin >> width;
    cout << "the area of ractangle is " << length * width;
    return 0;
}